= simple_form_for(@foundation) do |f|
  = f.error_notification

  .form-inputs
    = f.input :user_id
    = f.input :name
    = f.input :photo_url
    = f.input :description

  .form-actions
    = f.button :submit
